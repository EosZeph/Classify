# Security and Privacy

## Data handling

归类采集工作台没有服务器端组件，也不包含统计、广告或远程脚本。项目和采集内容通过 `chrome.storage.local` 保存在当前浏览器配置中，不会由本扩展主动上传。

扩展使用 `<all_urls>` 内容脚本匹配范围，以便用户在普通网页中选中文本后执行采集。扩展不会在未启动采集时写入网页内容。

## Permissions

- `storage`: 在本机保存项目、类别和采集内容。
- `tabs`: 打开和切换扩展工作台标签页。
- 网页匹配权限: 注入侧边采集台并监听用户主动选择的文本。

## User responsibility

使用者应确保其采集、保存和分享的数据具有合法授权，并遵守目标网站的服务条款、隐私政策、著作权和当地法律法规。不要使用本扩展采集未经授权的个人信息、敏感数据或受保护内容。

## Reporting

如发现安全问题，请在 GitHub 仓库中创建安全报告或联系作者 Eoszeph。请不要在公开 Issue 中披露尚未修复的漏洞细节和可利用数据。
